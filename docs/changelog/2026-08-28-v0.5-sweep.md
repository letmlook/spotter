# v0.5 末 — 大清扫 PR

> **Branch**: `evolve/post-sweep-cleanup` → `master`
> **Commits**: 15
> **Files changed**: 61 files, +5,367 / -190
> **Test status**: 14/14 包通过, `go test -race` 干净, 覆盖率 58.6% (CI 门槛 55%)

## 一句话

把"占位符 / stale / 假占位实现"全替成真活：12 个 `tracker` 上的 ⏳/🟡 项中 11 个标 ✅，加 1 个 PR 范围内的顺手清理（pre-existing 语法错）。

## 改动按主题分（review 时按这个顺序看）

### 1. 占位清理（最低风险、最先 review）

- `01fb145 docs: 清理占位与 spec 偏差登记`
  - `SPEC_DEVIATIONS.md` 把 DEVIATION-003 的 `PR #XXX` 占位填到 `0674e98`（git log 实际 commit）
  - v1.0 设计稿从 `2026-XX-XX-v1.0-design.md` 改名为 `2026-08-27-v1.0-design.md`
  - 删 `registry-server-design.md` 里指向不存在 plan 的死链
  - `operations.md(.en)` 明确 auto-update 推迟到 v1.1

- `22a3d75 fix(spotter-server): 修 pre-existing 语法错`
  - `cmd/spotter-server/main_test.go` 重写：之前是残骸（缺 3 个 `}`，3 个 test 粘连），4 个 test 干净分离（Healthz / ListDevicesEmpty / RegisterAndGet / GracefulShutdown）
  - 顺手收 `cmd/spotter-server/main.go` uncommitted 改动

- `1ec9363 test(scanner): WithAuthToken stamps Bearer header`
  - pre-existing untracked 单测，单独 commit 纳管

### 2. clientconfig 粘性身份

- `7f27015 feat(clientconfig): ClientID 粘性 UUID v4`
  - `internal/clientconfig.Settings.ClientID` 字段，json omitempty
  - `Open` 首次（文件缺失/空/corrupt）`uuid.NewString()` 生成 + 立即 flush 落盘
  - `Set` 时 caller 未传 → 保留 in-memory 已有 UUID（粘性身份）
  - `scanner.WithClientSenderID` 新 option；`main.go` wire
  - 4 个新测试（生成 / 跨 Open 持久 / Set 不覆盖 / 不同 store 互异）
  - **解**：agent 端能稳定识别同一 client（之前是固定字符串 `"spotter-client"`）

### 3. 电源操作闭环（agentd）

- `514d2f3 feat(agentd): /api/v1/power/cancel 真实实现`
  - 之前 501 占位。改为 in-memory `pending map[requestID]chan struct{}`
  - `delayExec` 三路 select 退出（ctx / cancelCh / timer）
  - 路由改 `POST /api/v1/power/cancel`（之前是 catch-all，GET 进 400；现在 405）
  - **顺手修 bug**：`r.Context()` 在 handler return 时被 cancel 立即触发 `delayExec` 退出，改为 `context.WithoutCancel` + `WithTimeout` 隔离
  - 4 个新 test + 修 1 个老 test
  - **解**：延迟重启的设备真的可以取消

- `9d5b38d feat(agentd): 电源审计 recent 端点 + 前端 PowerAuditList`
  - `internal/agentd/power.go` 加 `AuditEntry` struct + `Recent(n)` 方法（TSV 解码器，损坏行跳过不阻塞整页）
  - `GET /api/v1/power/audit/recent?limit=N`（默认 50，cap 200）
  - TSV 格式升级：`result=` → `status=`，统一 KV 形式
  - `scanner.FetchPowerAuditRecent` 转发 + 503 降级空列表
  - 前端 `PowerAuditList` 组件 + `DetailPanel` 集成（操作后 refetch）
  - 6 个新 audit test
  - **解**：UI 能看历史电源操作时间线

### 4. 时序 metrics（agentd）

- `c140c66 feat(agentd): 时序 metrics 端点 (CPU/内存/温度)`
  - 60 槽环形缓冲 + 5s 周期 sampler
  - `/proc/stat` diff 算 CPU% / `/proc/meminfo` 算 Mem% / `/sys/class/thermal/thermal_zone0` 读温度
  - `Agent.SetLifecycleContext` 注入（与 UDP/heartbeat loop 同一 ctx）
  - `GET /api/v1/metrics/recent` 端点
  - 6 个新测试
  - **解**：agent 端有 5 分钟历史，前端后续画 sparkline 用

### 5. log stream 增强（agentd）

- `a2dcc1d feat(agentd): log stream 增强 (multi-unit/grep/since/priority)`
  - `?unit=foo,bar` 多 unit 订阅 / `?grep=REGEX` / `?since=SPEC` / `?priority=LEVEL`
  - 默认 unit 始终在 `?unit=` 之前（不让 override 替换 agent 自己的日志）
  - `JournalctlOpts` struct 提到 build-tag-free 文件（`log_stream_opts.go`），跨平台可见
  - 2 个新 test
  - **解**：完整 journalctl 能力通过 HTTP 暴露

### 6. 客户端告警引擎

- `a09c647 feat(client): 客户端告警引擎 + AlertBanner`
  - 纯函数 `evaluate(devices, now)` 规则引擎
  - **v0.5 只实装** `device-unreachable`（5min 心跳超时，2x 阈值升级 critical）
  - **诚实降级**：`high-cpu` / `high-mem` / `high-temp` **未实装**——agent `Metrics` struct 是原始计数器（`cpu_seconds_total` / `mem_total_bytes` / `cpu_temp_c`），不是便利字段（`cpu_percent` / `mem_percent` / `temp_celsius`）。v0.6 需 agent 端 schema 扩展。
  - `AlertBanner` 组件按 critical > warning > info 排序
  - 测试基建：`scripts/run-tests.mjs`（`tsc` on-demand 编译 + `node --test`，绕过 npm vitest 4 ↔ vite 5 peer dep 冲突）
  - 9 个测试通过
  - **解**：UI 在 TitleBar 下显示告警横幅

### 7. agent `/admin` Web 监控

- `596cb30 feat(agentd): /admin 只读 HTML 监控页面`
  - 浏览器打开 `http://spotterd-host:9999/admin` 看 agent 自身（hostname / OS / kernel / arch / uptime / 网络 / Jetson / 实时指标 / 电源审计）
  - 零 JS / 零 CDN（embed.FS，< 300 KB）
  - `curl + lynx -dump` 一等公民
  - HTTP Basic auth 用 `cfg.Auth.Token`（与 JSON API 共享 secret）
  - `authMiddleware` 豁免 `/admin/*` 避免双重挑战
  - `GET /admin/metrics.json` JSON 端点（5min 历史，pipe `jq` 用）
  - 7 个新 test
  - **解**：operator 不用客户端也能在浏览器看 spotterd 状态

### 8. SQLite WAL 持久化（serverd）

- `ba929dc refactor(serverd): SQLite WAL 持久化（替换 JSON store）`
  - 选 `modernc.org/sqlite`（纯 Go，无 CGO 依赖）
  - WAL mode + `synchronous=NORMAL` + `busy_timeout(5000)`
  - `schema_version` 迁移框架
  - 接受旧 `.json` 路径自动重写为 `.db`（legacy 文件不动）
  - API 100% 兼容（Upsert/Get/List/Delete/MarkOffline）
  - 6 个新测试 + 16 goroutine × 100 并发写入验证
  - **解**：server 端从 PoC 升级到生产持久层

### 9. CI / DevX

- `0866c5e ci: 跨平台 client 编译 + cosign+SLSA + mDNS 设置`
  - `client-cross-compile.yml` 新文件：darwin/amd64（macos-13）、darwin/arm64（macos-14）、windows/amd64（windows-2022）
  - `release.yml` 增强：cosign v2.4.1 keyless signing（OIDC + Rekor）+ SLSA provenance + `homebrew-tap` 自动 PR
  - SettingsDialog 加 `enable_mdns` 字段（tracker P0-3 剩余）

- `5810c4b feat(scripts): OpenRC/runit init + macOS codesign/DMG`
  - `scripts/init/openrc/spotterd`（Alpine）、`scripts/init/runit/spotterd.run`（Void/Artix）
  - `scripts/macos/sign-and-notarize.sh` + `make-dmg.sh`（无 Apple 账号可完成，secrets gate）
  - `scripts/install.sh` 检测非 systemd 主机打印对应安装命令
  - `scripts/build-all.sh` 打包 `dist/scripts/init/`

### 10. docs + housekeeping

- `8dadfeb chore: ignore frontend/.test-build（tsc on-demand 输出）`

## 文档新增/更新

- `docs/macos-codesign.md` — 完整 operator 指南（secrets 配置 + 本地 dry-run + 故障排查）
- `docs/release-signing.md` — cosign verify 步骤（`cosign verify-blob` + SLSA `slsa-verifier`）
- `docs/admin.md` — `/admin` 端点 + 认证 + 故障排查
- `docs/api.md(.en)` — 新增章节：`/api/v1/power/audit/recent`、`/api/v1/metrics/recent`、log stream 新 query params
- `docs/install-mac.md` — 故障排查 + 维护流程
- `docs/operations.md(.en)` — auto-update 推迟到 v1.1 明确化
- `CLAUDE.md` — 状态表更新（发现周期 30s → 5s、CI 流程、覆盖率门槛）

## 测试覆盖

| 包 | 覆盖率 |
| --- | --- |
| `internal/agentd` | 54.9% |
| `internal/clientconfig` | 88.1% |
| `internal/jsonstore` | 86.7% |
| `internal/looputil` | 100.0% |
| `internal/registry` | 82.0% |
| `internal/scanner` | 69.1% |
| `internal/serverd` | 76.8% |
| `internal/timefmt` | 100.0% |
| `cmd/spotter-cli` | 80.5% |
| `cmd/spotter-server` | 45.8% |
| 根包 `spotter` | 22.2%（main.go Wails binding 不可单测）|
| **total** | **58.6%** |

CI 门槛 55%。每 PR 推高。

## 不在这次 PR 范围

- **C-8 客户端自更新**：留 v1.1（需 Apple Developer 账号 + diff 更新 + 跨平台签名链）。v1.0 用"GUI 检测按钮 → 浏览器下载"fallback。
- **v0.5 P0-3 spotterd → server 注册**：REST 端点已就绪，agent push heartbeat 路径未做。
- **v0.5 P2-8 设备分组**：告警引擎做了，**分组**没做。
- **v1.0 P0-1 macOS codesign**：框架已就位（`scripts/macos/sign-and-notarize.sh` + workflow 集成），需 Apple Developer Program secrets 激活。
- **v1.0 P0-2 原生菜单栏 + Cmd 快捷键**：Wails AppMenu API，未实装。
- **v1.0 P1-3 Homebrew tap**：公式已就位，需 `HOMEBREW_TAP_TOKEN` 触发 PR。

## Review checklist

- [ ] 电源操作 (`514d2f3`, `9d5b38d`) 涉及协议 + Wails binding + 前端组件，挨个查 diff
- [ ] `/admin` HTML 模板 (`596cb30`) 审查 template 的 field 引用与 admin.go 数据结构是否一致
- [ ] SQLite WAL (`ba929dc`) 确认 `cmd/spotter-server/main.go` 的 `Close()` 顺序在 `srv.Shutdown` 之后
- [ ] cosign workflow (`0866c5e`) review 三个新 job 的 permissions 和 continue-on-error
- [ ] `internal/agentd/power.go` 的 `r.Context()` 修复（`context.WithoutCancel`）— 重点核对

## 跟 review 同步

我（agent）做了一连串的"自主推进"，你（maintainer）做的是一连串的"继续 / 全部"。**我自己定位的"未做"项**（C-8 / 设备分组 / 设备→server 注册 / codesign 激活）已写在上面"不在这次 PR 范围"。**没标的可能遗漏**也请帮忙发现 — 我一个人 review 11 个 commit 容易看花眼。
